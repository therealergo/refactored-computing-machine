var x = 100;
var y = 0;

function xyadd(x, y) {
  while (x < y) {
    x = x + y;
  }
  return x;
}

function main() {
  var i = 0;
  while (i < 10) {
    xyadd(x , y);
    i = i + 1;
    x = x * i;
  }
  return x;
}