function divide(x, y) {
  if (y == 0)
    throw y;
  return x / y;
}

function main() {
  var r = 5;
  var s = 0;
  var t = 3;

  try {
    function add(x, y, z) {
      z = z + divide(x, y);
      return z;
    }
    return add(r, s, t);
  }
  catch(e) {
    s = e;
    return s;
  }
}